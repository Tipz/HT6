@inherits LayoutComponentBase
<MudThemeProvider Theme="theme" />
<MudPopoverProvider />
<MudDialogProvider />
<MudSnackbarProvider />
@Body
@code {
    private readonly MudTheme theme = new()
    {
        PaletteLight = new PaletteLight { Primary = "#2856b8", Error = "#b3261e", Background = "#f1f4f8", Surface = "#ffffff", TextPrimary = "#1f304c", TextSecondary = "#526278" },
        Typography = new Typography { Default = new DefaultTypography { FontFamily = ["Segoe UI", "sans-serif"] } }
    };
}
