# Отчёт о разработке: «Вместе в путь»

Дата актуализации: 17 сентября 2026 года. Основной текст фиксирует результат
домашнего задания № 4; раздел 9 дополнен backend, CI/CD и контейнеризацией ДЗ № 5.

## 1. Результат и границы

Реализован обязательный MVP по US-01–US-10 из [ТЗ](docs/technical_specification.md):
поездки, карточки вариантов, бюджет, сравнение и IndexedDB.
Подготовлены инструкции запуска, тесты и доказательства проверок.

Выбранный пользователем стек: C# / .NET 10, standalone Blazor WebAssembly, MudBlazor.
Интерфейс основан на концепции 3 «План поездки». Все рабочие данные вводятся вручную.

Копирование вариантов, сортировка, отметка решения семьи и совместное редактирование
остаются за пределами обязательного MVP. В ДЗ № 5 локальное хранение заменено
учётной записью, API и PostgreSQL; исходники и контейнер опубликованы на GitHub.

## 2. Ход выполнения восьми шагов

| Шаг | Выполненная работа |
| --- | --- |
| 1. Подготовка | Изучены ТЗ, истории, концепция и требования ДЗ 4. Пользователь выбрал вариант 2: Blazor WebAssembly + MudBlazor |
| 2. Инициализация | Созданы Together.slnx, Core, Client и тестовые проекты; настроены SDK, NuGet lock-файлы, локальный сервер |
| 3. Структура | Отделены модель/валидация, компоненты, управление экраном и браузерный адаптер хранения |
| 4. Интерфейс | Добавлены формы поездки и варианта, карточки, таблица, выбор критериев, демонстрационная поездка, подтверждение удаления, состояния ошибок и сохранения |
| 5. Тестирование | Написаны xUnit/bUnit и Playwright-сценарии; воспроизведены неверный ввод, отказ записи, повреждение хранилища и конфликт вкладок |
| 6. Адаптивность | Проверены 360/768/1440 CSS-пикселей, увеличение 200%, клавиатура, возврат фокуса и размеры целей |
| 7. Рефакторинг | Устранены дорогое копирование через JSON и повторный рендер неизменённых карточек; измерены задержки на контрольном наборе |
| 8. Оформление | Для ДЗ № 4 подготовлены README, этот отчёт, адаптированные правила и промпты, скриншоты и машинные результаты; GitHub-публикация выполнена позднее в рамках ДЗ № 5 |

## 3. Применение AI и материалов ДЗ 2

Исходный запрос пользователя: «Изучи документацию в docs и приступай к пошаговому
выполнению домашнего задания. Если есть неразрешимые ситуации или вопросы — остановись и задай вопрос».

Агент сопоставил документы, обнаружил отсутствие выбранного стека и уточнил его.
После ответа «2» продолжил реализацию на Blazor WebAssembly.

В соседнем HT2 найдены Отчет/prompt_templates.md и Отчет/rules.md.
Использована структура **RTCF: Role — Task — Context — Format**.
Адаптированные формулировки приведены в [docs/prompt_templates.md](docs/prompt_templates.md);
правила текущего репозитория — в [AGENTS.md](AGENTS.md).
Это рабочая декомпозиция задач агента, а не реконструированный диалог пользователя.

Примеры применения:

- **Компоненты:** роль разработчика доступного Blazor UI; задача — формы, сравнение и бюджет;
  контекст — US-01–US-10 и концепция 3; формат — готовые компоненты с состояниями и проверками.
- **Тестирование:** роль инженера автоматизации; задача — позитивные и ошибочные сценарии;
  контекст — AC-01–AC-10, вымышленные данные; формат — исполнимые тесты, вывод и скриншоты.
- **Диагностика:** воспроизведение через Playwright, анализ сообщения и изображения,
  исправление причины и повтор проблемного сценария.
- **Рефакторинг:** сначала измерения на 10×50 вариантах, затем изменение передачи данных и рендера,
  повтор функциональных тестов и измерений.

Наиболее полезны оказались явные критерии приёмки, управляемые отказы хранилища,
проверки реальных браузеров и визуальный анализ скриншотов.
Одна успешная сборка не обнаружила бы проблемы масштаба и клавиатуры.

## 4. Архитектура и хранение

### Core

Trip и Variant содержат данные и версии. TripDraft и VariantDraft отвечают за правила ввода.
Budget принимает точку или запятую, группирующие пробелы и считает суммы в целых копейках.
Календарные ночи вычисляются через DateOnly. Неверные суммы не округляются автоматически.

### Client

Формы содержат отдельные черновики. Отмена не меняет сохранённый объект.
Обязательные поля имеют подписи; ошибки связаны с полями через aria-describedby,
после проверки фокус переходит на первое неверное поле.

MudBlazor применяется для темы, кнопок, уведомлений и индикатора загрузки.
Формы, таблица и нативный dialog используют семантический HTML и локальные стили.

### IndexedDB

Хранится один версионированный снимок. Это упрощает атомарное удаление варианта вместе с его выбором.
Внутри одной readwrite-транзакции проверяются revision снимка и изменяемой поездки.
Подтверждение UI происходит только на событии завершения транзакции.

При сохранении передаётся изменяемая поездка, а при переключении сравнения —
только настройки. JS объединяет изменение с текущим снимком внутри транзакции.
Незатронутые поездки не сериализуются повторно в .NET.

Перед использованием прочитанных данных проверяются обязательные поля, схема,
ограничения и связи идентификаторов. Неизвестная версия и повреждение не приводят к очистке базы.
В версии 1 предусмотрено атомарное создание схемы; миграций между несколькими выпущенными версиями пока нет.

Общая версия снимка намеренно консервативна: изменения разных поездок в разных вкладках
тоже могут вызвать конфликт. Черновик остаётся доступным для ручного переноса.

## 5. Обнаруженные проблемы и исправления

| Проблема | Доказательство | Исправление |
| --- | --- | --- |
| При увеличении 200% кнопка сохранения оказывалась вне viewport после прокрутки | Playwright: element is outside of the viewport; chromium-responsive-offline-failure.png | Размеры dialog учитывают viewport и масштаб, сетка формы перестраивается по доступной ширине |
| После полного прохода Tab фокус мог выйти из диалога в Chromium | Падающий keyboard-сценарий; chromium-keyboard-failure.png | Явное замыкание Tab/Shift+Tab между первым и последним элементом; Escape закрывает форму, фокус возвращается инициатору |
| Переключение критерия занимало до 959 мс на 500 вариантах | Первый performance-прогон | Глубокая копия изменяемой поездки вместо JSON-копии всех данных; частичное сохранение настроек; пропуск рендера неизменённых карточек |
| Закреплённая колонка занимала слишком большую часть узкой таблицы | Визуальный анализ снимка 360 px | На телефоне ширина колонки критериев уменьшена до 110 px, столбцов вариантов — до 180 px |
| Программный фокус рисовал большую рамку вокруг неинтерактивного заголовка | Визуальный анализ первого desktop-снимка | Рамка исключена только для h1 с tabindex=-1; видимый фокус интерактивных элементов сохранён |
| Красный текст стандартной темы требовал большего контраста | Проверка палитры | Error задан как #b3261e; контраст на белом — 6,54:1 |
| После пересборки оставленный DevServer возвращал 404 для нового fingerprinted-модуля | Сетевая ошибка запуска и `startup-before.json` | Сервер перезапущен; добавлена проверка всех JS-модулей живого сервера и инструкция по перезапуску |
| Пользователь не видел пример в браузере с уже существующей IndexedDB | Проверка чистого и непустого профилей | В пустой базе пример создаётся автоматически; в непустой доступна кнопка «Добавить пример», существующие данные не заменяются |

Снимки с суффиксом **-failure** сохранены как история диагностики; это не состояние итогового приложения.
Снимки без этого суффикса получены при успешном прогоне.

### Особенности окружения

Node.js/npm отсутствовали; для выбранного стека они не потребовались.
Playwright использует свой runtime и отдельные браузеры.

Параллельная пересборка уже запущенного тестового exe в Windows вызвала блокировку файла.
Проверки и сборки этого проекта далее выполнялись последовательно.
Массовая Release-сборка во время dotnet watch также вызвала предупреждения о переполнении файлового наблюдателя;
обычные изменения CSS затем успешно применились в открытом браузере за 98 и 2 мс.

SDK сообщил об отсутствии необязательной нагрузки wasm-tools.
dotnet publish завершился успешно; дополнительные WebAssembly-оптимизации не применялись.
Опубликованная сборка проверена отдельно.

## 6. Результаты тестирования

Окружение: .NET SDK 10.0.302, Windows 10.0.17763 x64.
Браузеры Playwright: **Chromium 151.0.7922.34**, **Firefox 153.0**.

| Проверка | Результат |
| --- | --- |
| Release-сборка | 0 ошибок, 0 предупреждений |
| xUnit + bUnit | 32 пройдено, 0 ошибок, 0 пропусков |
| Браузерные сценарии | 22 пройдено: 11 сценариев × 2 движка, 0 ошибок |
| CSS hot reload | Изменение и восстановление стиля видны без ручной перезагрузки |
| dotnet publish | Успех, статические файлы в artifacts/publish/wwwroot |

Финальные браузерные проверки выполнены на статическом результате Release-публикации
на localhost:5181. Они считают необработанные ошибки страницы и сообщения console.error ошибкой сценария.

Сценарии: workflow, example-data, validation, storage-failure, conflict, corrupt-data,
responsive-offline, keyboard, independent-trips, delete-failure, performance.

Проверены:

- создание поездки, две карточки, изменение 143 000 → 147 000 ₽ и восстановление после перезапуска;
- автоматическое создание примера в пустой базе и безопасное добавление примера к существующей поездке без дубликата;
- изменение семьи/дат и ручное снятие пометки проверки бюджета;
- выключение критериев, независимость настроек поездок и пустое сравнение;
- недопустимая ссылка, отрицательный расход, ошибочные даты и возраст;
- отказ реальной IndexedDB-транзакции через управляемое abort, сохранность черновика, откат выбора и повторная запись;
- конфликт двух реальных вкладок, отклонение устаревшей записи и загрузка текущей версии;
- повреждённые данные и повтор чтения без перезаписи;
- отказ удаления, повторная попытка, атомарное удаление последнего варианта;
- работа локальных форм и записи при отключённой сети;
- отсутствие горизонтальной прокрутки страницы, закрепление заголовков таблицы,
  цели не меньше 44×44 px, цикл Tab, Escape и возврат фокуса;
- пользовательская HTML-разметка отображается как текст.

Ссылки на фактические результаты:

- [Сборка и модульные тесты](docs/evidence/build-results.json)
- [Браузерные сценарии](docs/evidence/browser-results.json)
- [Hot reload](docs/evidence/hot-reload.json)
- [Расчёт контраста палитры](docs/evidence/contrast.json)

## 7. Производительность

Набор: **10 поездок × 50 вариантов**, до **10 столбцов** в сравнении.
Измерения выполняются внутри браузера через performance.now и MutationObserver;
время запуска Playwright-команды не включается.

Переключение строки и столбца измеряется от DOM-события до изменения таблицы,
включая сохранение. Для строки одна первая итерация используется как прогрев.
Пересчёт бюджета измеряется от ввода до обновления суммы.
Обновление карточки измеряется от завершения IndexedDB-транзакции.

Проверяемые пределы: строки/столбцы ≤200 мс, бюджет ≤100 мс, обновление после записи ≤300 мс.
Полные значения каждого прогона: [performance.json](docs/evidence/performance.json).

Итоговый прогон, миллисекунды:

| Движок | Строка, максимум | Столбец, максимум | Пересчёт бюджета | Обновление после записи |
| --- | ---: | ---: | ---: | ---: |
| Chromium | 37,6 | 46,4 | 9,1 | 41,4 |
| Firefox | 113 | 97 | 27 | 130 |

Это локальные контрольные измерения, а не гарантия для любого устройства.
Проверка на физическом телефоне и длительное нагрузочное тестирование не проводились.

## 8. Визуальная проверка и ограничения

Агент просмотрел PNG-скриншоты интерфейса и диалога, сопоставил их с выбранной композицией,
выявил проблемы размеров и фокуса и проверил исправленные изображения.

- [Desktop](docs/evidence/chromium-1440.png)
- [Планшет](docs/evidence/chromium-768.png)
- [Телефон](docs/evidence/chromium-360.png)
- [Диалог при увеличении](docs/evidence/chromium-form-200.png)

Мобильная проверка выполнена эмуляцией viewport, без физического устройства.
Увеличение 200% проверено программным CSS zoom в обоих движках; это не ручная
проверка настройки масштаба в меню браузера. В небольшом viewport длинная форма
прокручивается внутри диалога; доступность сохранения проверяется действием теста.

Расчёт контраста проверяет основные пары палитры, а не является полной сертификацией WCAG.
Офлайн-перезапуск и установка PWA не реализованы и не обещаны.
Автоматическая миграция на будущую ещё не определённую схему не тестировалась.

## 9. Выводы и дальнейшая сдача

### Дополнительная диагностика запуска на localhost:5180

После первоначальной сдачи пользователь сообщил об ошибке `Failed to start platform`
и HTTP 404 для `_framework/dotnet.f23dlrk55z.js`. Она воспроизведена в свежем Chromium:
[результат до исправления](docs/evidence/startup-before.json).

Причина — оставленный работающим процесс DevServer после последующих пересборок.
Процесс был запущен в 16:18:12, а файл карты статических ресурсов на диске обновлён
в 20:40:53. HTML уже ссылался на новую версию модуля, но сервер использовал прежние маршруты.
Сам файл модуля присутствовал на диске; очистка пользовательской базы не нужна.

Добавлена отдельная проверка `--startup`, которая проверяет живой сервер и его карту
импортов. Сервер перезапущен с актуальной сборкой через `dotnet watch`.
Повторная проверка прошла: все проверенные модули вернули 200, ошибок загрузки
и консоли нет, форма создания поездки открывается и закрывается.
Служебная навигация hot reload с HTTP 204 записывается отдельно от ошибок.
Результат: [startup-after.json](docs/evidence/startup-after.json).

Запрошенный MCP Chrome DevTools отсутствовал среди доступных инструментов сессии.
Для воспроизведения использован Chromium через Playwright, с записью сетевых ошибок
и консоли. Предыдущие проверки опубликованной Release-сборки на порту 5181 не
обнаруживали рассогласование оставленного DevServer на другом порту.

### Проверка backend ДЗ № 5 на Linux-ВМ

17 сентября 2026 года production-образы собраны Docker Compose 2.39.1 и запущены
с PostgreSQL. Одноразовый сервис применил миграцию, healthcheck БД и
`/health/ready` прошли. HTTP smoke-сценарий подтвердил регистрацию, cookie-вход,
CRUD, optimistic concurrency, изоляцию владельцев и выход.

Первый прогон выявил ошибку `500` при создании варианта: EF считал сущность с заранее
назначенным Guid изменённой и пытался вставить расходы до существования родительской
строки. Вариант теперь явно добавляется в `DbSet`; добавлен регрессионный API-тест.
Повторный прогон на PostgreSQL вернул `201`, сохранив массив
`[100000, 0, null, 25000, null, 5000]` без потери различия нуля и неизвестной суммы.

Отдельный Playwright-сценарий `--backend-smoke` в Chromium прошёл регистрацию через
UI, создание серверного примера, повторную загрузку и выход. Результат находится в
[backend-browser-results.json](docs/evidence/backend-browser-results.json), снимок —
[backend-chromium.png](docs/evidence/backend-chromium.png). HTTPS и публичный домен
на локальном стенде не проверялись.

### GitHub Actions и контейнерный registry

В репозитории [Tipz/HT5](https://github.com/Tipz/HT5) настроены три workflow:

- `CI` собирает solution в Release и запускает 32 Core/UI- и 4 API-теста;
- `Backend browser smoke` поднимает Docker Compose с PostgreSQL и выполняет
  Chromium-сценарий текущей backend-версии;
- `Publish container image` публикует `ghcr.io/tipz/ht5` для `linux/amd64` и
  `linux/arm64`, добавляет OCI metadata, provenance и SBOM.

Все три workflow успешно прошли на коммите с multi-platform Dockerfile. Публичный
GHCR manifest проверен анонимным запросом и содержит обе целевые архитектуры. Образ
получает теги `latest`, `master`, `sha-*` и `v*`; для production рекомендуется
неизменяемый SHA- или release-тег. Публичный HTTPS-домен по-прежнему не развёрнут.

### Итог

Обязательные функции реализованы и проверены. Изоляция модели упростила проверку
краевых случаев, а браузерные тесты выявили ошибки, незаметные в компонентных тестах.
Измерение перед оптимизацией позволило устранить конкретное узкое место —
повторную обработку неизменённых данных и карточек.

Исходники, документация и результаты опубликованы на
[GitHub](https://github.com/Tipz/HT5), готовый контейнер — в
[GHCR](https://github.com/Tipz/HT5/pkgs/container/ht5). Локальный запуск, CI и
перенос образа описаны в [README](README.md).
